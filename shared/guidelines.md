### Input
- Each step should have its own independent input fields
- Inputs are optional unless explicitly required
- The system should handle incomplete inputs gracefully

### Output
- Responses must be:
  - Professional
  - Clear and concise
  - Easy to scan and digest
- Structure outputs in a way that aligns with the step’s requirements

### Requirements
- All outputs must strictly follow the criteria defined in each step’s `### Requirements` section
